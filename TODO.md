# TODO

**Tasks live on the board**, not here:
<https://virta.tosijs.net/host/#?virta.scope=tosijs-editor> — or `virta brief` in
this directory, which a SessionStart hook already runs for you.

Everything that was in this file was imported on 2026-09-26 (23 items, 11 of them
already done). File new work with `virta create`, not by editing this file: an
item added here reaches nobody, and the next import would duplicate it.

`ready` is the owner's go-ahead. Agents file into the backlog and leave it there.

The pre-import contents are in git history if you need them:
`git show v0.5.1 -- TODO.md`.
